{
  "name": "insightst-design",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter playground --",
    "build": "turbo run build",
    "docs": "turbo run dev --filter docs --",
    "clean": "node scripts/clean.js",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "turbo": "^2.9.14"
  }
}
